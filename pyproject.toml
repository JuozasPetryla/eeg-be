[project]
name = "eeg-be"
version = "0.1.0"
description = "EEG backend API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.6"
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"